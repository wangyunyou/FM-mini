# 生产环境模板 —— 本文件会提交，只放占位值。
# 真实域名走 .env.production.local（不入库）或 CI 环境变量注入，不要提交到仓库。
# 线上地址必须是 HTTPS，且已在微信公众平台配好 request 合法域名。

TARO_APP_API_BASE_URL=https://REPLACE_WITH_YOUR_FM_HOST

# AppID 留空 = 沿用 project.config.json 里写死的值；线上要用另一个小程序号时在这里或 .local 里覆盖。
TARO_APP_ID=
